#ifndef __VARIABLE_H__
#define __VARIABLE_H__

#include "header.h"

extern FILE * fp_sent_data;
extern FILE * fp_received_data;
extern FILE * fpw;
extern char str_line_sent[50];
extern char str_line_received[50];

#endif