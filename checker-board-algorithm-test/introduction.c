#include "header.h"

void introduction(FILE * fp)
{
	fputs("===Checkerboard Algorithm Memory Test === \n", fp);
	fputs("// Compare between the sent and received data // \n", fp);
	fputs("// If they are same, the last bit will be one (or TRUE) // \n", fp);
	fputs("// Otherwise, it will be zero (or FALSE) // \n\n", fp);
}